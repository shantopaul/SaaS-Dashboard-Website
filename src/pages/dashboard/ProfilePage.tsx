import { PageShell } from "../PageShell";

export function ProfilePage() {
  return (
    <PageShell
      badge="Protected Route"
      description="This placeholder confirms the /dashboard/profile route works. Profile forms and account details will be built in Feature 27."
      primaryHref="/"
      primaryLabel="Back home"
      title="Profile Page"
    />
  );
}
