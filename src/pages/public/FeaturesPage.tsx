import { PageShell } from "../PageShell";

export function FeaturesPage() {
  return (
    <PageShell
      badge="Public Route"
      description="This placeholder confirms the /features route works. The detailed feature grid will be built in Feature 08."
      primaryHref="/pricing"
      primaryLabel="View pricing route"
      title="Features Page"
    />
  );
}
