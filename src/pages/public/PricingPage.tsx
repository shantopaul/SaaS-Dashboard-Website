import { PageShell } from "../PageShell";

export function PricingPage() {
  return (
    <PageShell
      badge="Public Route"
      description="This placeholder confirms the /pricing route works. Pricing cards, comparison, and FAQ content will be built in Feature 09."
      primaryHref="/login"
      primaryLabel="Go to login route"
      title="Pricing Page"
    />
  );
}
